name: tests
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  test: ^1.16.8
  bloc: ^7.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test: 
    sdk: flutter
    version: ^0.0.0
  mocktail: ^0.1.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/
